import { Box, Container, Grid } from "@mui/material"
import { Link } from "react-router-dom"

export default function Feed() {
    return (
      <Box flex={12} bgcolor='blue' sx={{display: {xs:'block'}}}>
        <Container>
            <Grid />
          
        </Container>
      </Box> 
    )
}