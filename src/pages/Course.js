import { Box, Container } from "@mui/material"


export default function Contratacion({user, course}) {
    return (
        <Box flex={12}>
            <Container>
                Pagina de clase
            </Container>
        </Box>
    )
}